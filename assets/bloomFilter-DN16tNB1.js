import{c as e,n as t,r as n,t as r}from"./cn--uKEoY85.js";import{t as i}from"./x-Db0QUW9_.js";import{b as a,n as o,r as s,t as c,x as l}from"./index-Ck1ZSPKq.js";var u=a(`circle-question-mark`,[[`circle`,{cx:`12`,cy:`12`,r:`10`,key:`1mglay`}],[`path`,{d:`M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3`,key:`1u773s`}],[`path`,{d:`M12 17h.01`,key:`p32p05`}]]),d=e(n(),1),f=t(),p=24,m=[{name:`apple`,bits:[1,5,14]},{name:`banana`,bits:[3,9,14]},{name:`cherry`,bits:[7,12,20]},{name:`date (never added)`,bits:[5,9,12]},{name:`elderberry (never added)`,bits:[2,10,22]}];function h(){let[e,t]=(0,d.useState)(Array(p).fill(!1)),[n,a]=(0,d.useState)(new Set),[h,g]=(0,d.useState)(null);function _(e){t(t=>{let n=[...t];return e.bits.forEach(e=>n[e]=!0),n}),a(t=>new Set(t).add(e.name))}function v(t){let r=t.bits.every(t=>e[t]);g({name:t.name,verdict:r?`maybe`:`no`,falsePositive:r&&!n.has(t.name)})}return(0,f.jsxs)(`div`,{className:`space-y-5`,children:[(0,f.jsxs)(o,{children:[(0,f.jsxs)(`div`,{className:`mb-3 text-sm font-semibold text-text`,children:[`Bit array (`,p,` bits, 3 hash functions per item)`]}),(0,f.jsx)(`div`,{className:`grid grid-cols-8 gap-1.5`,children:e.map((e,t)=>(0,f.jsx)(l.div,{animate:{backgroundColor:e?`rgba(99,102,241,0.5)`:`rgba(255,255,255,0.04)`},className:`flex h-7 items-center justify-center rounded font-mono text-[9px] text-text`,children:t},t))})]}),(0,f.jsxs)(o,{children:[(0,f.jsx)(`div`,{className:`mb-3 text-sm font-semibold text-text`,children:`Items`}),(0,f.jsx)(`div`,{className:`space-y-2`,children:m.map(e=>(0,f.jsxs)(`div`,{className:`flex items-center justify-between gap-2 rounded-lg border border-border bg-white/[0.02] px-3 py-2`,children:[(0,f.jsx)(`span`,{className:r(`font-mono text-xs`,n.has(e.name)?`text-success`:`text-muted`),children:e.name}),(0,f.jsxs)(`div`,{className:`flex gap-2`,children:[(0,f.jsx)(c,{size:`sm`,variant:`secondary`,onClick:()=>_(e),disabled:n.has(e.name),children:`Add`}),(0,f.jsx)(c,{size:`sm`,onClick:()=>v(e),children:`Check`})]})]},e.name))}),h&&(0,f.jsxs)(l.div,{initial:{opacity:0,y:6},animate:{opacity:1,y:0},className:r(`mt-4 flex items-center gap-2 rounded-lg border p-3 text-xs`,h.verdict===`no`?`border-success/30 bg-success/5 text-success`:`border-warning/30 bg-warning/5 text-warning`),children:[h.verdict===`no`?(0,f.jsx)(i,{size:14}):(0,f.jsx)(u,{size:14}),(0,f.jsxs)(`span`,{children:[`"`,h.name,`" is`,` `,h.verdict===`no`?`definitely NOT in the set`:`MAYBE in the set (all bits happen to be set)`,h.falsePositive&&(0,f.jsx)(s,{tone:`danger`,className:`ml-2`,children:`false positive!`})]})]}),(0,f.jsx)(`p`,{className:`mt-3 text-xs text-muted`,children:`A Bloom filter never has false negatives — if it says "not in the set," that's certain. But it can have false positives, as shown above when an item's bits happen to already be set by others.`})]})]})}var g={visual:h,howItWorks:[{title:`A bit array plus k hash functions`,description:`A Bloom filter is a fixed-size array of bits, all initially 0. To add an item, it's run through k independent hash functions, and each resulting bit position is set to 1.`},{title:`Checking membership`,description:`To check if an item might be in the set, hash it the same way and check all k bit positions. If any bit is 0, the item is definitely not in the set. If all bits are 1, it's probably in the set.`,code:`def mightContain(item):
    return all(bits[hash_i(item)] for i in range(k))`},{title:`False positives are possible, false negatives are not`,description:`Because bits can be set by multiple different items, an item that was never added can still have all its bits happen to be set by others — a false positive. But a bit that's 0 is definitively 0, so 'not in the set' is always correct.`},{title:`Tuning the false positive rate`,description:`The false positive rate depends on the bit array size, the number of hash functions, and the number of items added — bigger array and well-tuned k keep the false positive rate low even with millions of items.`},{title:`You can't remove items (without a counting variant)`,description:`Because bits are shared across items, clearing a bit to 'remove' one item could break membership checks for other items that also set that bit. A Counting Bloom Filter solves this with counters instead of single bits.`}],tradeoffs:{pros:[`Extremely space-efficient — a fraction of a byte per item, regardless of item size`,`O(k) constant-time add and lookup, independent of the number of items stored`,`No false negatives — a definitive 'not present' answer is always trustworthy`],cons:[`False positives are possible and must be tolerated by the calling system`,`Standard Bloom filters don't support deletion`,`Doesn't store the actual items — only membership, no way to enumerate what's in the set`],whenToUse:[`Preventing unnecessary lookups: checking a Bloom filter before hitting a database or a slow downstream service`,`Malicious URL / spam filtering (Chrome's Safe Browsing), duplicate detection at scale`],whenNotToUse:[`Anywhere a false positive is unacceptable and can't be double-checked against a source of truth`,`When you need to enumerate or remove individual items frequently`],alternatives:[{name:`Counting Bloom Filter`,note:`Uses small counters instead of single bits, enabling deletion`},{name:`Cuckoo filter`,note:`Similar space efficiency with support for deletion and often better false-positive rates`},{name:`HyperLogLog`,note:`Solves a different problem — cardinality estimation rather than membership testing`}]},interviewAnswer:{script:`I'd use a Bloom filter as a cheap pre-check in front of an expensive lookup — for example, in the URL shortener design, checking whether a short code definitely doesn't exist before querying the database, to avoid cache/database penetration attacks. I'd be precise about the guarantee: no false negatives, so a 'definitely not present' answer can always be trusted and skip the expensive lookup; but false positives are possible, so a 'maybe present' answer still requires falling through to the real data store to confirm. I'd also mention that standard Bloom filters can't support deletion — if that's needed, a Counting Bloom Filter or Cuckoo filter would be the fix.`,mistakes:[`Claiming a Bloom filter can definitively confirm an item IS in the set (it only gives 'maybe')`,`Not knowing that standard Bloom filters can't support deletion`,`Using a Bloom filter where the actual item values need to be retrieved or enumerated`],followUps:[`How would you support deletion from a Bloom filter?`,`How do you choose the bit array size and number of hash functions for a target false positive rate?`,`What's the difference between a Bloom filter and a HyperLogLog?`],redFlags:[`Not knowing what a false positive means in this context`,`Confusing a Bloom filter with a hash set that stores actual values`]},challenge:[{question:`If a Bloom filter says an item is 'definitely not in the set,' how confident can you be in that answer?`,options:[`Not confident at all — it could still be a false negative`,`Completely confident — Bloom filters never produce false negatives`,`50/50 — it's a coin flip`,`It depends on the number of hash functions`],correctIndex:1,explanation:`A Bloom filter's 'not in the set' answer is always correct — false negatives are impossible by construction, since a real item would have set all of its bits when added.`},{question:`Why can't a standard Bloom filter easily support removing an item?`,options:[`Bloom filters don't use bits at all`,`Clearing a bit for one item could incorrectly clear a bit that another item also depends on`,`Removal requires re-hashing the entire filter`,`It actually can, trivially`],correctIndex:1,explanation:`Because multiple items can share the same bit positions, clearing a bit to remove one item risks breaking membership checks for other items that also set that same bit.`}]};export{g as bloomFilter};