import{c as e,n as t,r as n,t as r}from"./cn--uKEoY85.js";import{t as i}from"./ban-eDBsOrgv.js";import{t as a}from"./circle-lXW-YTUx.js";import{t as o}from"./database-fYfkXIOw.js";import{C as s,D as c,n as l,t as u,y as d}from"./index-Cq8ggJSO.js";var f=e(n(),1),p=t(),m=10;function h(){let[e,t]=(0,f.useState)(m),[n,h]=(0,f.useState)(2),[g,_]=(0,f.useState)(null),[v,y]=(0,f.useState)(!1),[b,x]=(0,f.useState)([5,5,5]);(0,f.useEffect)(()=>{let e=setInterval(()=>{t(e=>Math.min(m,e+n*.5)),x(e=>e.map(e=>Math.min(5,e+n*.5)))},500);return()=>clearInterval(e)},[n]);function S(){e>=1?(t(e=>e-1),_(`ok`)):_(`limited`)}function C(e){x(t=>{let n=[...t];return n[e]>=1&&--n[e],n})}return(0,p.jsxs)(`div`,{className:`space-y-5`,children:[(0,p.jsxs)(l,{children:[(0,p.jsxs)(`div`,{className:`mb-3 flex items-center justify-between`,children:[(0,p.jsx)(`div`,{className:`text-sm font-semibold text-text`,children:`Token bucket`}),(0,p.jsxs)(`span`,{className:`font-mono text-xs text-muted`,children:[`refill: `,n,`/s`]})]}),(0,p.jsx)(`input`,{type:`range`,min:.5,max:5,step:.5,value:n,onChange:e=>h(Number(e.target.value)),className:`w-full accent-primary`}),(0,p.jsx)(`div`,{className:`mt-4 flex items-center justify-center gap-1.5`,children:Array.from({length:m}).map((t,n)=>(0,p.jsx)(`div`,{children:n<Math.floor(e)?(0,p.jsx)(d,{size:18,className:`text-success`}):(0,p.jsx)(a,{size:18,className:`text-border`})},n))}),(0,p.jsxs)(`div`,{className:`mt-1 text-center font-mono text-xs text-muted`,children:[Math.floor(e),` / `,m,` tokens`]}),(0,p.jsxs)(`div`,{className:`mt-4 flex items-center justify-center gap-3`,children:[(0,p.jsx)(u,{size:`sm`,onClick:S,children:`Send request`}),(0,p.jsx)(c,{mode:`wait`,children:g&&(0,p.jsxs)(s.span,{initial:{opacity:0,y:-6},animate:{opacity:1,y:0},className:r(`flex items-center gap-1 text-xs font-medium`,g===`ok`?`text-success`:`text-danger`),children:[g===`ok`?(0,p.jsx)(d,{size:14}):(0,p.jsx)(i,{size:14}),g===`ok`?`200 OK`:`429 Too Many Requests`]},g+Math.random())})]})]}),(0,p.jsxs)(l,{children:[(0,p.jsxs)(`div`,{className:`mb-3 flex items-center justify-between`,children:[(0,p.jsx)(`div`,{className:`text-sm font-semibold text-text`,children:`Distributed rate limiting`}),(0,p.jsx)(`button`,{onClick:()=>y(e=>!e),className:r(`rounded-full border px-3 py-1 text-xs font-medium transition-colors`,v?`border-primary/50 bg-primary/15 text-indigo-300`:`border-border text-muted`),children:v?`Redis-backed shared bucket`:`Naive per-node buckets`})]}),v?(0,p.jsxs)(p.Fragment,{children:[(0,p.jsxs)(`div`,{className:`flex items-center justify-center gap-4`,children:[[0,1,2].map(e=>(0,p.jsxs)(`div`,{className:`rounded-lg border border-border bg-white/[0.02] px-3 py-2 text-xs text-muted`,children:[`Server `,e+1]},e)),(0,p.jsx)(`span`,{className:`text-muted`,children:`→`}),(0,p.jsxs)(`div`,{className:`flex flex-col items-center gap-1 rounded-lg border border-primary/40 bg-primary/10 px-4 py-3`,children:[(0,p.jsx)(o,{size:16,className:`text-indigo-300`}),(0,p.jsx)(`span`,{className:`text-xs text-indigo-300`,children:`Redis: shared counter`})]})]}),(0,p.jsx)(`p`,{className:`mt-3 text-xs text-muted`,children:`All servers check and decrement the same atomic counter in Redis (via INCR + TTL, or a Lua script), so the limit is enforced globally regardless of which server handles the request.`})]}):(0,p.jsxs)(p.Fragment,{children:[(0,p.jsx)(`div`,{className:`flex justify-around gap-3`,children:b.map((e,t)=>(0,p.jsxs)(`button`,{onClick:()=>C(t),className:`flex flex-col items-center gap-1 rounded-lg border border-border bg-white/[0.02] px-4 py-3`,children:[(0,p.jsxs)(`span`,{className:`text-xs text-muted`,children:[`Server `,t+1]}),(0,p.jsxs)(`span`,{className:`font-mono text-sm text-text`,children:[Math.floor(e),` tokens`]})]},t))}),(0,p.jsx)(`p`,{className:`mt-3 text-xs text-warning`,children:`Each node enforces its own limit independently — a client hitting all 3 servers can get 3x the intended global rate limit.`})]})]})]})}var g={visual:h,howItWorks:[{title:`Token bucket: the most common algorithm`,description:`A bucket holds up to N tokens. Each request consumes one token; tokens refill continuously at a fixed rate. If the bucket is empty, the request is rejected with a 429.`,code:`if bucket.tokens >= 1:
    bucket.tokens -= 1
    return allow()
return reject(429)`},{title:`Bursts are allowed, sustained rate is capped`,description:`Because tokens can accumulate up to the bucket's capacity while idle, a client can burst up to that capacity instantly, but is capped to the refill rate over any sustained period.`},{title:`Sliding window vs fixed window`,description:`Fixed window (e.g. 'max 100 requests per calendar minute') is simple but allows a 2x burst at window boundaries. Sliding window log/counter algorithms smooth this out at the cost of more memory or computation.`},{title:`Rate limiting must be centralized across nodes`,description:`If each app server keeps its own in-memory counter, a client can get N times the intended limit by spreading requests across N servers. A shared store (Redis) with an atomic INCR + TTL fixes this.`,code:`local key = KEYS[1]
local count = redis.call('INCR', key)
if count == 1 then redis.call('EXPIRE', key, 60) end
return count <= tonumber(ARGV[1])`},{title:`Where to enforce it`,description:`Rate limiting can live at the API gateway/edge (protects the whole system, coarse-grained), or per-service/per-endpoint (fine-grained, e.g. stricter limits on expensive endpoints).`}],tradeoffs:{pros:[`Protects backend services from being overwhelmed by a single client or bug`,`Enables fair usage across customers on shared infrastructure`,`Token bucket specifically allows reasonable bursts without being overly strict`],cons:[`Adds a dependency (Redis or similar) if enforced consistently across many nodes`,`Poorly tuned limits create false positives that frustrate legitimate users`,`Fixed window algorithms allow boundary bursts if not carefully designed`],whenToUse:[`Any public API to prevent abuse and ensure fair usage`,`Protecting expensive downstream operations (search, ML inference, third-party API calls)`],whenNotToUse:[`Purely internal, trusted, low-traffic service-to-service calls where the operational overhead isn't justified`],alternatives:[{name:`Leaky bucket`,note:`Processes requests at a constant output rate regardless of input burstiness`},{name:`Fixed window counter`,note:`Simplest to implement, but allows a 2x burst at window boundaries`},{name:`Sliding window log`,note:`Most accurate, tracks exact timestamps, but higher memory cost per key`}]},interviewAnswer:{script:`I'd implement rate limiting with a token bucket per API key, stored in Redis so the limit is enforced consistently regardless of which app server handles the request — using an atomic Lua script or Redis's INCR+EXPIRE to avoid race conditions. I'd pick token bucket specifically because it allows reasonable bursts (useful for legitimate clients doing batch operations) while still capping the sustained rate. For a multi-tenant API like Stripe's, I'd also expose the remaining quota via response headers so clients can back off proactively instead of hitting 429s blindly.`,mistakes:[`Implementing rate limiting with per-server in-memory counters in a multi-server deployment`,`Not handling the race condition when multiple requests check-then-decrement the counter concurrently`,`Choosing a fixed window without acknowledging the boundary-burst problem`],followUps:[`How do you avoid a race condition when two requests check the counter at the same time?`,`How would you rate limit per-user versus per-IP, and why might you need both?`,`What should the API return when a client is rate limited?`],redFlags:[`Not knowing what a 429 status code means`,`Suggesting per-server counters work fine in a horizontally scaled deployment`]},challenge:[{question:`Why does a token bucket algorithm allow short bursts of traffic while still enforcing a long-term rate limit?`,options:[`It doesn't — it always rejects any burst`,`Tokens accumulate up to the bucket's capacity while idle, so a client can spend them all at once, but refill rate caps sustained throughput`,`It only limits requests during business hours`,`It ignores request rate entirely`],correctIndex:1,explanation:`The bucket's capacity allows instantaneous bursts up to that size, while the refill rate governs the maximum sustained rate over time.`},{question:`Why is a per-server, in-memory rate limit counter problematic in a horizontally scaled deployment?`,options:[`It's not problematic at all`,`A client can bypass the intended global limit by spreading requests across multiple servers, each with its own independent counter`,`In-memory counters are always slower than Redis`,`It only works for GET requests`],correctIndex:1,explanation:`Without a shared, centralized counter (like Redis), each server enforces its own local limit, letting a client multiply their effective global limit by the number of servers.`}]};export{g as rateLimiting};