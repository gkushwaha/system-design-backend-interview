import{c as e,n as t,r as n,t as r}from"./cn--uKEoY85.js";import{t as i}from"./database-f1NFtwp3.js";import{t as a}from"./user-DEJr_E_w.js";import{C as o,n as s,r as c}from"./index-B6k1PGct.js";var l=e(n(),1),u=t(),d=[{id:`strong`,label:`Strong`,delays:[0,0,0],note:`The write isn't acknowledged until every replica has it — every read everywhere sees the latest value immediately.`},{id:`read-your-writes`,label:`Read-your-writes`,delays:[0,2,3],note:`Your own session (pinned to replica A) always sees your write instantly. Other users on other replicas catch up over time.`},{id:`causal`,label:`Causal`,delays:[1,2,3],note:`Propagation still takes time, but causally related writes (e.g. a comment after its photo) always arrive in the correct relative order at every replica.`},{id:`eventual`,label:`Eventual`,delays:[1,3,4],note:`No ordering or timing guarantee at all — replicas converge 'eventually' given no new writes, on their own schedule.`}],f=4;function p(){let[e,t]=(0,l.useState)(`strong`),[n,p]=(0,l.useState)(0),m=d.find(t=>t.id===e);return(0,u.jsx)(`div`,{className:`space-y-5`,children:(0,u.jsxs)(s,{children:[(0,u.jsx)(`div`,{className:`mb-3 flex flex-wrap gap-2`,children:d.map(n=>(0,u.jsx)(`button`,{onClick:()=>t(n.id),className:r(`rounded-full border px-3 py-1 text-xs font-medium transition-colors`,e===n.id?`border-primary/50 bg-primary/15 text-indigo-300`:`border-border text-muted hover:text-text`),children:n.label},n.id))}),(0,u.jsx)(`p`,{className:`mb-4 text-xs text-muted`,children:m.note}),(0,u.jsxs)(`div`,{className:`mb-2 flex items-center gap-2 rounded-lg border border-border bg-white/[0.02] p-3 text-xs text-muted`,children:[(0,u.jsx)(a,{size:14,className:`text-indigo-300`}),` Client writes `,(0,u.jsx)(`span`,{className:`font-mono text-text`,children:`value = "V2"`}),` at t=0`]}),(0,u.jsxs)(`div`,{className:`mb-3 flex items-center justify-between text-xs text-muted`,children:[(0,u.jsx)(`span`,{children:`Time since write`}),(0,u.jsxs)(`span`,{className:`font-mono text-text`,children:[`t = `,n]})]}),(0,u.jsx)(`input`,{type:`range`,min:0,max:f,value:n,onChange:e=>p(Number(e.target.value)),className:`w-full accent-primary`}),(0,u.jsx)(`div`,{className:`mt-4 grid grid-cols-3 gap-3`,children:m.delays.map((t,r)=>{let a=n>=t;return(0,u.jsxs)(o.div,{animate:{borderColor:a?`rgba(34,197,94,0.6)`:`rgba(30,30,46,1)`},className:`flex flex-col items-center gap-2 rounded-lg border bg-white/[0.02] p-3`,children:[(0,u.jsx)(i,{size:20,className:a?`text-success`:`text-muted`}),(0,u.jsxs)(`span`,{className:`text-[10px] text-muted`,children:[`Replica `,String.fromCharCode(65+r),` `,r===0&&e===`read-your-writes`&&`(your session)`]}),(0,u.jsx)(c,{tone:a?`success`:`warning`,children:a?`V2 (fresh)`:`V1 (stale)`})]},r)})})]})})}var m={visual:p,howItWorks:[{title:`Strong consistency`,description:`A write is not acknowledged as successful until every replica has applied it. Every subsequent read, from any replica, sees the latest value. Simple to reason about, but limits availability and adds latency.`},{title:`Read-your-writes consistency`,description:`A weaker, pragmatic guarantee: a user always sees their own writes immediately (typically by pinning their session's reads to the replica or leader that handled their write), even if other users see it later.`},{title:`Causal consistency`,description:`Operations that are causally related (a comment can't appear before the post it comments on) are guaranteed to be seen in that order everywhere. Unrelated operations may still be seen in different orders on different replicas.`,code:`// causally related — must be seen in this order everywhere
post = createPost()
comment = createComment(post.id)`},{title:`Eventual consistency`,description:`The weakest common guarantee: if no new writes occur, all replicas will eventually converge to the same value — but with no bound on how long, and no ordering guarantee in the meantime.`},{title:`Choosing a model is a product decision, not just technical`,description:`The 'right' model depends on what a stale read actually costs the user — a stale follower count is harmless; a stale bank balance is not.`}],tradeoffs:{pros:[`Weaker models (eventual, causal) enable higher availability and lower latency`,`Read-your-writes gives a strong-feeling user experience without full strong consistency's cost`,`Causal consistency prevents confusing 'effect before cause' bugs cheaply`],cons:[`Strong consistency limits availability during partitions and adds cross-replica coordination latency`,`Eventual consistency can surface confusing, hard-to-reproduce bugs if the UI doesn't account for staleness`,`Read-your-writes requires session affinity or clever routing, adding infrastructure complexity`],whenToUse:[`Strong — financial balances, inventory counts, anything where staleness causes real harm`,`Read-your-writes — social apps where users expect to see their own actions reflected instantly`,`Causal — comment threads, chat, anything with clear cause-effect relationships`,`Eventual — like counts, view counts, presence indicators where slight staleness is invisible to users`],whenNotToUse:[`Don't default to strong consistency everywhere — it's the most expensive option and most data doesn't need it`,`Don't use pure eventual consistency for anything where a user would notice or be harmed by seeing stale data`],alternatives:[{name:`Quorum consistency (R+W>N)`,note:`A tunable middle ground between strong and eventual, configurable per operation`},{name:`CRDTs`,note:`Data structures designed to merge conflicting concurrent writes automatically without central coordination`}]},interviewAnswer:{script:`I'd pick the consistency model per piece of data, not for the whole system. For a bank balance or inventory count, I'd want strong consistency even at some latency cost. For a social feed's like count, eventual consistency is invisible to users and much cheaper. For a user's own posts or comments, I'd specifically call out read-your-writes — routing a user's reads to the replica that handled their write, or the leader, so they never see their own action appear to 'disappear'. I'd also mention causal consistency for anything with clear cause-effect ordering, like a comment thread, since users would immediately notice a comment appearing before the post it replies to.`,mistakes:[`Treating consistency as one global system-wide setting instead of a per-data-type decision`,`Not knowing what read-your-writes means or why it matters for perceived UX quality`,`Assuming eventual consistency means data is ever actually 'wrong' rather than just temporarily stale`],followUps:[`How would you implement read-your-writes without routing every read to the leader?`,`What's a concrete example of a causal consistency violation a user would notice?`,`How does quorum consistency let you tune between strong and eventual?`],redFlags:[`Insisting everything should just use strong consistency 'to be safe'`,`Not being able to give a concrete user-facing example of why consistency choice matters`]},challenge:[{question:`Which consistency model specifically guarantees a user always sees their own submitted comment immediately, even if other users see it moments later?`,options:[`Eventual consistency`,`Read-your-writes consistency`,`Causal consistency`,`None of these guarantee this`],correctIndex:1,explanation:`Read-your-writes consistency specifically guarantees that a client sees the effects of its own writes immediately, typically via session-pinned routing.`},{question:`What does causal consistency guarantee that plain eventual consistency does not?`,options:[`That all replicas are always perfectly in sync`,`That causally related operations (like a post and its comment) are seen in the same relative order by every replica`,`That writes are never lost`,`That reads are always faster`],correctIndex:1,explanation:`Causal consistency specifically preserves the relative order of operations that have a cause-effect relationship, even though unrelated operations may still be seen in different orders across replicas.`}]};export{m as consistencyModels};