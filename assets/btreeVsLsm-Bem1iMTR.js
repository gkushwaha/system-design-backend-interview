import{c as e,n as t,r as n}from"./cn--uKEoY85.js";import{t as r}from"./layers-CRMCxFVg.js";import{T as i,n as a,r as o,t as s,x as c}from"./index-CWyuo5Pt.js";var l=e(n(),1),u=t(),d=20;function f(){let[e,t]=(0,l.useState)(0),[n,f]=(0,l.useState)([]),[p,m]=(0,l.useState)(0),[h,g]=(0,l.useState)([]),[_,v]=(0,l.useState)(0),[y,b]=(0,l.useState)(!1);function x(){let e=Math.floor(Math.random()*d);f(t=>[...t,e].slice(-6)),t(e=>e+1)}function S(){m(e=>{let t=e+1;return t>=4?(g(e=>{let t=[...e,1];return t.length>=3?(b(!0),setTimeout(()=>{v(e=>e+1),g([]),b(!1)},700),t):t}),0):t})}return(0,u.jsx)(`div`,{className:`space-y-5`,children:(0,u.jsxs)(`div`,{className:`grid grid-cols-1 gap-4 sm:grid-cols-2`,children:[(0,u.jsxs)(a,{children:[(0,u.jsxs)(`div`,{className:`mb-2 flex items-center justify-between`,children:[(0,u.jsx)(`div`,{className:`text-sm font-semibold text-text`,children:`B-tree — in-place writes`}),(0,u.jsxs)(o,{tone:`danger`,children:[e,` random I/Os`]})]}),(0,u.jsx)(`div`,{className:`grid grid-cols-5 gap-1.5`,children:Array.from({length:d}).map((e,t)=>(0,u.jsx)(c.div,{animate:{backgroundColor:n.includes(t)?`rgba(239,68,68,0.5)`:`rgba(255,255,255,0.03)`},transition:{duration:.5},className:`h-6 rounded-sm border border-border`},t))}),(0,u.jsx)(s,{size:`sm`,className:`mt-3`,onClick:x,children:`Write a row`}),(0,u.jsx)(`p`,{className:`mt-2 text-xs text-muted`,children:`Each write updates a page wherever the key sorts to — scattered, random disk I/O.`})]}),(0,u.jsxs)(a,{children:[(0,u.jsxs)(`div`,{className:`mb-2 flex items-center justify-between`,children:[(0,u.jsx)(`div`,{className:`text-sm font-semibold text-text`,children:`LSM-tree — sequential writes`}),y&&(0,u.jsx)(o,{tone:`warning`,children:`Compacting…`})]}),(0,u.jsxs)(`div`,{className:`space-y-2`,children:[(0,u.jsxs)(`div`,{children:[(0,u.jsx)(`div`,{className:`mb-1 text-[10px] text-muted`,children:`Memtable (in-memory)`}),(0,u.jsx)(`div`,{className:`h-3 w-full overflow-hidden rounded-full bg-white/5`,children:(0,u.jsx)(`div`,{className:`h-full rounded-full bg-primary transition-all`,style:{width:`${p/4*100}%`}})})]}),(0,u.jsxs)(`div`,{children:[(0,u.jsx)(`div`,{className:`mb-1 text-[10px] text-muted`,children:`Level 0 (flushed SSTables)`}),(0,u.jsx)(`div`,{className:`flex gap-1`,children:(0,u.jsx)(i,{children:h.map((e,t)=>(0,u.jsx)(c.div,{initial:{opacity:0,scale:.5},animate:{opacity:1,scale:1},exit:{opacity:0},className:`flex h-7 w-9 items-center justify-center rounded bg-success/20 text-success`,children:(0,u.jsx)(r,{size:12})},t))})})]}),(0,u.jsxs)(`div`,{children:[(0,u.jsx)(`div`,{className:`mb-1 text-[10px] text-muted`,children:`Level 1 (compacted)`}),(0,u.jsx)(`div`,{className:`flex gap-1`,children:Array.from({length:_}).map((e,t)=>(0,u.jsx)(`div`,{className:`flex h-7 w-9 items-center justify-center rounded bg-warning/20 text-warning`,children:(0,u.jsx)(r,{size:12})},t))})]})]}),(0,u.jsx)(s,{size:`sm`,className:`mt-3`,onClick:S,children:`Write a row`}),(0,u.jsx)(`p`,{className:`mt-2 text-xs text-muted`,children:`Writes append to an in-memory memtable, flushed sequentially to disk, then merged (compacted) into higher levels in the background.`})]})]})})}var p={visual:f,howItWorks:[{title:`B-tree: update in place`,description:`A B-tree write finds the correct sorted position on disk and updates that page directly. Reads are excellent (few page hops), but writes require random disk I/O — expensive if pages aren't already cached.`},{title:`LSM-tree: append, don't update`,description:`A write to an LSM-tree first lands in an in-memory memtable — a fast, purely sequential operation with no disk seek at all.`},{title:`Flushing to immutable SSTables`,description:`Once the memtable fills up, it's flushed to disk as a new, immutable, sorted string table (SSTable) file — again, a fast sequential write, never modifying existing files.`,code:`memtable.flush() → level0/sstable_004.sst  (immutable, sorted)`},{title:`Background compaction merges levels`,description:`Periodically, a background process merges multiple SSTables together, discarding overwritten/deleted keys and producing fewer, larger sorted files at the next level — this is where write amplification comes from.`},{title:`Reads must check multiple places`,description:`A read may need to check the memtable, then each relevant SSTable level (often using a Bloom filter per SSTable to skip files that definitely don't contain the key) — this is the LSM-tree's read amplification tradeoff.`}],tradeoffs:{pros:[`B-tree: excellent read performance, in-place updates, mature and predictable`,`LSM-tree: excellent write throughput since writes are always sequential, never blocking on random disk seeks`],cons:[`B-tree: writes require random I/O, which is slow on spinning disks and adds wear on SSDs`,`LSM-tree: reads can be slower (must check multiple levels), and background compaction consumes I/O and CPU (write amplification)`],whenToUse:[`B-tree — read-heavy workloads, traditional relational databases (Postgres, MySQL InnoDB)`,`LSM-tree — write-heavy workloads: time series, event logging, Cassandra, RocksDB, LevelDB`],whenNotToUse:[`Don't use a pure LSM-tree design for latency-critical point reads without a Bloom filter and good level tuning`,`Don't use a B-tree for extremely high, sustained write throughput on spinning disks — random I/O becomes the bottleneck`],alternatives:[{name:`B+ tree`,note:`Variant used by most relational databases, storing all values in leaf nodes for efficient range scans`},{name:`Log-structured merge with tiered vs leveled compaction`,note:`Different compaction strategies trade write vs read vs space amplification differently`}]},interviewAnswer:{script:`I'd choose based on the read/write ratio. For a read-heavy relational workload, like most OLTP applications, a B-tree (what Postgres and MySQL use) gives excellent read performance with in-place updates. For a write-heavy workload — ingesting time series data or event logs — an LSM-tree (what Cassandra and RocksDB use) is a better fit, since writes are always sequential appends rather than random in-place updates. I'd also mention the tradeoff explicitly: LSM-trees push cost into background compaction and slightly higher read amplification, in exchange for much better write throughput.`,mistakes:[`Not knowing which real databases use which structure (Postgres/MySQL = B-tree, Cassandra/RocksDB = LSM-tree)`,`Ignoring compaction entirely when discussing LSM-trees — it's a first-class operational concern, not a footnote`,`Assuming LSM-trees are strictly 'better' rather than a different point on the read/write tradeoff curve`],followUps:[`What is write amplification and why do LSM-trees have more of it?`,`How does a Bloom filter help LSM-tree read performance?`,`What operational concerns come with running an LSM-tree database (compaction storms, tombstones)?`],redFlags:[`Not knowing what compaction is`,`Claiming B-trees can't handle any meaningful write load`]},challenge:[{question:`Why do LSM-trees generally offer better write throughput than B-trees?`,options:[`LSM-trees never write to disk`,`Writes are always sequential appends (memtable, then flushed SSTables) instead of random in-place page updates`,`LSM-trees use less storage`,`B-trees don't support writes at all`],correctIndex:1,explanation:`LSM-tree writes avoid random disk seeks entirely by always appending sequentially, which is dramatically faster than a B-tree's random in-place page updates, especially at high write volume.`},{question:`What is 'write amplification' in the context of an LSM-tree?`,options:[`The fact that writes are encrypted`,`The extra disk I/O caused by background compaction rewriting data across levels multiple times`,`The process of duplicating writes across data centers`,`A bug that causes duplicate writes`],correctIndex:1,explanation:`Because compaction repeatedly reads and rewrites data as it merges SSTables into higher levels, the same logical write ends up physically written to disk multiple times over its lifetime.`}]};export{p as btreeVsLsm};