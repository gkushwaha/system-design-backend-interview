import{c as e,n as t,r as n}from"./cn--uKEoY85.js";import{t as r}from"./database-Lh3LwOP0.js";import{r as i}from"./Tabs-DjcIOGSj.js";import{_ as a,b as o,n as s,r as c,t as l,w as u,y as d}from"./index-C8EiWwXu.js";var f=d(`send`,[[`path`,{d:`M14.536 21.686a.5.5 0 0 0 .937-.024l6.5-19a.496.496 0 0 0-.635-.635l-19 6.5a.5.5 0 0 0-.024.937l7.93 3.18a2 2 0 0 1 1.112 1.11z`,key:`1ffxy3`}],[`path`,{d:`m21.854 2.147-10.94 10.939`,key:`12cjpa`}]]),p=e(n(),1),m=t();function h(){let[e,t]=(0,p.useState)(`idle`),[n,d]=(0,p.useState)(`idle`);function h(){t(`db-ok-mq-fail`)}function g(){d(`written`),setTimeout(()=>d(`relayed`),1200)}return(0,m.jsx)(`div`,{className:`space-y-5`,children:(0,m.jsxs)(`div`,{className:`grid grid-cols-1 gap-4 sm:grid-cols-2`,children:[(0,m.jsxs)(s,{className:`border-danger/30`,children:[(0,m.jsx)(`div`,{className:`mb-2 text-sm font-semibold text-text`,children:`Naive dual write`}),(0,m.jsxs)(`div`,{className:`flex items-center justify-center gap-3 rounded-lg border border-border bg-white/[0.02] p-4`,children:[(0,m.jsxs)(`div`,{className:`flex flex-col items-center gap-1`,children:[(0,m.jsx)(r,{size:20,className:`text-success`}),(0,m.jsx)(`span`,{className:`text-[10px] text-muted`,children:`DB write`}),e!==`idle`&&(0,m.jsx)(a,{size:12,className:`text-success`})]}),(0,m.jsx)(`span`,{className:`text-muted`,children:`+`}),(0,m.jsxs)(`div`,{className:`flex flex-col items-center gap-1`,children:[(0,m.jsx)(f,{size:20,className:e===`db-ok-mq-fail`?`text-danger`:`text-muted`}),(0,m.jsx)(`span`,{className:`text-[10px] text-muted`,children:`Publish event`}),e===`db-ok-mq-fail`&&(0,m.jsx)(i,{size:12,className:`text-danger`})]})]}),(0,m.jsx)(l,{size:`sm`,className:`mt-3`,onClick:h,children:`Simulate order creation`}),e===`db-ok-mq-fail`&&(0,m.jsx)(o.p,{initial:{opacity:0},animate:{opacity:1},className:`mt-3 text-xs text-danger`,children:`The DB write committed, but the message broker call failed (network blip, broker down). The order exists — but no downstream service was ever notified. Silent data loss.`})]}),(0,m.jsxs)(s,{className:`border-success/30`,children:[(0,m.jsx)(`div`,{className:`mb-2 text-sm font-semibold text-text`,children:`Transactional outbox`}),(0,m.jsxs)(`div`,{className:`flex items-center justify-center gap-3 rounded-lg border border-border bg-white/[0.02] p-4`,children:[(0,m.jsxs)(`div`,{className:`flex flex-col items-center gap-1`,children:[(0,m.jsx)(r,{size:20,className:`text-success`}),(0,m.jsx)(`span`,{className:`text-[10px] text-muted`,children:`DB + outbox row`}),n!==`idle`&&(0,m.jsx)(a,{size:12,className:`text-success`})]}),(0,m.jsx)(`span`,{className:`text-muted`,children:`→`}),(0,m.jsxs)(`div`,{className:`flex flex-col items-center gap-1`,children:[(0,m.jsx)(f,{size:20,className:n===`relayed`?`text-success`:`text-muted`}),(0,m.jsx)(`span`,{className:`text-[10px] text-muted`,children:`Relay publishes`}),(0,m.jsx)(u,{children:n===`relayed`&&(0,m.jsx)(o.div,{initial:{opacity:0},animate:{opacity:1},children:(0,m.jsx)(a,{size:12,className:`text-success`})})})]})]}),(0,m.jsx)(l,{size:`sm`,className:`mt-3`,onClick:g,children:`Simulate order creation`}),n!==`idle`&&(0,m.jsxs)(`div`,{className:`mt-3 space-y-1 text-xs`,children:[(0,m.jsx)(c,{tone:n===`relayed`?`success`:`warning`,children:n===`relayed`?`Event relayed successfully`:`Order + outbox row written atomically…`}),(0,m.jsxs)(`p`,{className:`text-muted`,children:[`The order and an 'OrderCreated' outbox row are written in the `,(0,m.jsx)(`em`,{children:`same`}),` database transaction — so they either both happen or neither does. A separate relay process then reads unpublished outbox rows and publishes them, retrying until it succeeds.`]})]})]})]})})}var g={visual:h,howItWorks:[{title:`The dual write problem`,description:`A service often needs to both update its own database AND publish an event about that change (e.g. save an order, then publish 'OrderCreated'). These are two separate systems — there's no way to make them succeed or fail together with a naive approach.`},{title:`Write the event to an outbox table, in the same transaction`,description:`Instead of publishing directly to the message broker, the service writes the event as a row in an 'outbox' table, inside the exact same database transaction as the business data change.`,code:`BEGIN;
  INSERT INTO orders (...) VALUES (...);
  INSERT INTO outbox (event_type, payload) VALUES ('OrderCreated', ...);
COMMIT;`},{title:`A separate relay process publishes outbox rows`,description:`A background process (or a CDC tool like Debezium reading the database's write-ahead log) polls the outbox table for unpublished rows, publishes them to the message broker, and marks them as sent.`},{title:`At-least-once delivery, deduplicated downstream`,description:`If the relay crashes after publishing but before marking a row as sent, it might publish the same event twice on restart — so downstream consumers need to handle duplicate events idempotently.`},{title:`Why this solves the dual write problem`,description:`Because the business data and the outbox event are written in one atomic database transaction, they can never diverge — either both happen, or neither does. The message broker publish becomes a reliable, retryable background step instead of a fragile inline call.`}],tradeoffs:{pros:[`Eliminates the silent-data-loss window where a DB write succeeds but the event publish fails`,`Keeps the event log durable and replayable even if the message broker is temporarily down`,`Works with any database that supports transactions — no exotic infrastructure required`],cons:[`Adds an extra table and a relay process that must itself be monitored and kept running`,`Introduces latency between the DB write and the event actually being published`,`Consumers must handle duplicate events (at-least-once delivery), adding idempotency requirements downstream`],whenToUse:[`Any service that must reliably publish an event whenever it changes its own data (order creation, payment processing, inventory updates)`,`Systems already using CDC (Debezium) where reading the outbox table via the WAL is nearly free`],whenNotToUse:[`Simple internal tools where an occasional missed event is genuinely inconsequential`,`Systems that can tolerate using a full distributed transaction (2PC) instead, though that has its own costs`],alternatives:[{name:`Change Data Capture (CDC)`,note:`Tools like Debezium read the DB's write-ahead log directly, treating the whole table as an implicit outbox`},{name:`Two-phase commit (2PC)`,note:`A more general but heavier-weight distributed transaction protocol across the DB and broker`},{name:`Event sourcing`,note:`Skips the dual-write problem structurally by making the event log itself the source of truth`}]},interviewAnswer:{script:`I'd use the outbox pattern anywhere a service needs to atomically update its own data and reliably notify other services — for example, an order service that must publish 'OrderCreated' once an order is saved. Rather than calling the message broker directly after the DB commit (which can silently fail, losing the event), I'd write the event as a row in an outbox table within the same database transaction as the order insert. A separate relay process — ideally a CDC tool like Debezium reading the database's write-ahead log — then publishes outbox rows to Kafka and marks them sent. I'd flag that this gives at-least-once delivery, so downstream consumers need to be idempotent against duplicate events.`,mistakes:[`Publishing directly to the message broker right after the DB commit, without acknowledging the dual-write failure window`,`Not having a plan for what happens if the relay process crashes after publishing but before marking the row sent`,`Assuming the outbox pattern gives exactly-once delivery rather than at-least-once`],followUps:[`How would you prevent the relay process from becoming a bottleneck or single point of failure?`,`How do downstream consumers handle a duplicate event caused by an outbox relay retry?`,`How does using Debezium/CDC differ from a custom polling relay process?`],redFlags:[`Not recognizing the dual-write problem as a real, distinct failure mode`,`Proposing a distributed transaction across the DB and message broker as if that were simple to implement`]},challenge:[{question:`What specific problem does the transactional outbox pattern solve?`,options:[`It makes database writes faster`,`The dual-write problem — ensuring a DB update and a message publish either both happen or neither does`,`It replaces the need for a message queue entirely`,`It encrypts messages in transit`],correctIndex:1,explanation:`The outbox pattern makes the event-publishing step part of the same atomic database transaction as the business data change, closing the window where one could succeed while the other silently fails.`},{question:`Why must downstream consumers of outbox-relayed events be idempotent?`,options:[`They don't need to be — outbox guarantees exactly-once delivery`,`The relay process can publish the same event more than once if it crashes between publishing and marking it sent, giving at-least-once delivery`,`Idempotency is unrelated to the outbox pattern`,`Because the outbox table has no primary key`],correctIndex:1,explanation:`If the relay crashes after successfully publishing but before recording that success, it will retry and publish the same event again on restart — consumers need to handle that duplicate safely.`}]};export{g as outboxPattern};