import{c as e,n as t,r as n}from"./cn--uKEoY85.js";import{t as r}from"./rotate-cw-DkH9fJoD.js";import{_ as i,b as a,n as o,r as s,t as c,y as l}from"./index-CCRajdmP.js";var u=l(`file`,[[`path`,{d:`M6 22a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h8a2.4 2.4 0 0 1 1.704.706l3.588 3.588A2.4 2.4 0 0 1 20 8v12a2 2 0 0 1-2 2z`,key:`1oefj6`}],[`path`,{d:`M14 2v5a1 1 0 0 0 1 1h5`,key:`wfsgrz`}]]),d=e(n(),1),f=t(),p=6;function m(){let[e,t]=(0,d.useState)(Array(p).fill(`idle`)),[n,l]=(0,d.useState)(!1);function m(){l(!1),t(Array(p).fill(`uploading`)),e.forEach((e,n)=>{setTimeout(()=>{t(e=>{let t=[...e];return t[n]=n===2?`failed`:`done`,t})},500+n*250)})}function h(e){t(t=>{let n=[...t];return n[e]=`uploading`,n}),setTimeout(()=>{t(t=>{let n=[...t];return n[e]=`done`,n})},700)}let g=e.every(e=>e===`done`);return(0,f.jsxs)(`div`,{className:`space-y-5`,children:[(0,f.jsxs)(o,{children:[(0,f.jsxs)(`div`,{className:`mb-3 flex items-center justify-between`,children:[(0,f.jsx)(`div`,{className:`text-sm font-semibold text-text`,children:`Multipart upload — 6 parts in parallel`}),(0,f.jsx)(c,{size:`sm`,onClick:m,children:`Start upload`})]}),(0,f.jsx)(`div`,{className:`grid grid-cols-3 gap-3 sm:grid-cols-6`,children:e.map((e,t)=>(0,f.jsxs)(`div`,{className:`flex flex-col items-center gap-1.5 rounded-lg border border-border bg-white/[0.02] p-2`,children:[(0,f.jsxs)(`span`,{className:`text-[9px] text-muted`,children:[`Part `,t+1]}),(0,f.jsxs)(a.div,{animate:{backgroundColor:e===`done`?`rgba(34,197,94,0.2)`:e===`failed`?`rgba(239,68,68,0.2)`:`rgba(99,102,241,0.15)`},className:`flex h-9 w-9 items-center justify-center rounded`,children:[e===`done`&&(0,f.jsx)(i,{size:16,className:`text-success`}),e===`failed`&&(0,f.jsx)(r,{size:16,className:`text-danger`}),e===`uploading`&&(0,f.jsx)(a.div,{animate:{rotate:360},transition:{repeat:1/0,duration:1,ease:`linear`},className:`h-3 w-3 rounded-full border-2 border-primary border-t-transparent`}),e===`idle`&&(0,f.jsx)(`span`,{className:`h-2 w-2 rounded-full bg-border`})]}),e===`failed`&&(0,f.jsx)(c,{size:`sm`,variant:`secondary`,onClick:()=>h(t),children:`Retry`})]},t))}),(0,f.jsxs)(`div`,{className:`mt-4 flex items-center gap-3`,children:[(0,f.jsx)(c,{size:`sm`,disabled:!g,onClick:()=>l(!0),children:`Complete multipart upload`}),n&&(0,f.jsxs)(a.div,{initial:{opacity:0,scale:.8},animate:{opacity:1,scale:1},className:`flex items-center gap-2`,children:[(0,f.jsx)(u,{size:18,className:`text-success`}),(0,f.jsx)(s,{tone:`success`,children:`Object assembled`})]})]}),(0,f.jsx)(`p`,{className:`mt-3 text-xs text-muted`,children:`Only the failed part needs to be retried — not the whole file. This is why multipart upload is standard for large objects (video, backups): parallelism plus cheap partial retry.`})]}),(0,f.jsxs)(o,{children:[(0,f.jsx)(`div`,{className:`mb-2 text-sm font-semibold text-text`,children:`Consistency model`}),(0,f.jsxs)(`p`,{className:`text-xs text-muted`,children:[`Since December 2020, S3 provides `,(0,f.jsx)(`strong`,{className:`text-text`,children:`strong read-after-write consistency`}),` `,`for all operations — a read immediately after a PUT or DELETE always reflects the latest state. Before that, overwrite/delete operations were only eventually consistent, which is worth knowing since it's still commonly asked about historically.`]})]})]})}var h={visual:m,howItWorks:[{title:`Objects, not files or blocks`,description:`S3 is object storage: every object is a blob of data plus metadata, addressed by a flat key within a bucket — there's no real directory hierarchy, just keys that look like paths.`},{title:`Multipart upload splits large objects`,description:`For large files, the client splits the upload into multiple parts (5MB-5GB each) and uploads them in parallel, each independently, dramatically speeding up large uploads and allowing retry of just the failed part.`,code:`createMultipartUpload() → uploadId
uploadPart(uploadId, partNumber=1..N)  // in parallel
completeMultipartUpload(uploadId, [partETags])`},{title:`Only failed parts need retry`,description:`If one part fails to upload (network blip), only that part needs to be re-sent — not the entire multi-gigabyte file — which is the main practical benefit over a single monolithic upload.`},{title:`CompleteMultipartUpload assembles the object`,description:`Once all parts are uploaded, a final API call tells S3 to assemble them into one logical object. The object doesn't become visible/downloadable until this completion step succeeds.`},{title:`Strong read-after-write consistency`,description:`Since December 2020, S3 guarantees that a read immediately after a successful PUT, POST, or DELETE reflects that change — no more eventual consistency window to design around, though it's a commonly asked historical detail.`}],tradeoffs:{pros:[`Multipart upload parallelizes large transfers and makes partial failure cheap to recover from`,`Object storage scales effectively infinitely with no capacity planning from the user's side`,`Strong consistency removes a whole historical class of 'why don't I see my upload yet' bugs`],cons:[`Object storage isn't a filesystem — no atomic rename, no partial in-place edits, no real directories`,`Multipart upload adds client-side complexity (tracking part ETags, orchestrating parallel uploads)`,`Listing objects by prefix can be slower than a real filesystem directory listing at very large scale`],whenToUse:[`Any object over ~100MB — AWS recommends multipart upload starting around this size`,`User-generated content, backups, video, static assets — anything that doesn't need in-place mutation`],whenNotToUse:[`Workloads needing a true POSIX filesystem with in-place edits and directory semantics (use EBS or EFS instead)`,`Very small objects where multipart's coordination overhead outweighs the parallelism benefit`],alternatives:[{name:`EBS`,note:`Block storage — a virtual disk attached to one instance, supports in-place writes`},{name:`EFS`,note:`Network file storage with POSIX semantics, shareable across many instances`}]},interviewAnswer:{script:`For large file uploads — video, backups — I'd use multipart upload: split the file into parts, upload them in parallel for speed, and if any single part fails, only that part needs retrying instead of restarting the whole transfer. I'd call out that the object isn't visible until CompleteMultipartUpload succeeds, so partial uploads never leave a corrupt half-object visible to readers. On consistency, I'd mention that modern S3 gives strong read-after-write consistency for all operations — worth knowing explicitly since older material (and some interviewers) still reference the historical eventual consistency model for overwrites, which was fixed industry-wide in December 2020.`,mistakes:[`Not knowing that S3 now provides strong consistency, citing outdated eventual-consistency behavior as current`,`Treating S3 as if it supports in-place file edits like a real filesystem`,`Not mentioning multipart upload for large file design questions`],followUps:[`How would you resume a large upload that got interrupted partway through?`,`What's the difference between object, block, and file storage?`,`How would you handle generating a pre-signed URL so a client can upload directly to S3?`],redFlags:[`Not knowing what an object store is, or confusing it with a traditional filesystem`]},challenge:[{question:`What is the main practical benefit of multipart upload for a large file?`,options:[`It compresses the file automatically`,`Parts upload in parallel and only a failed part needs to be retried, not the entire file`,`It encrypts the file in transit`,`It reduces the total storage cost`],correctIndex:1,explanation:`Multipart upload speeds up large transfers via parallelism and makes partial failure cheap — you only re-upload the part that failed, not the whole object.`},{question:`As of the current S3 consistency model, what happens if you read an object immediately after successfully overwriting it?`,options:[`You might see the old version for a while (eventual consistency)`,`You always see the new version immediately (strong read-after-write consistency)`,`The read fails until you wait 24 hours`,`S3 does not support overwriting objects`],correctIndex:1,explanation:`Since December 2020, S3 provides strong read-after-write consistency for all operations, including overwrites — a read right after a successful write always reflects that write.`}]};export{h as s3Internals};